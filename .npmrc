# Swizzled theme components under src/theme import transitive Docusaurus
# packages (e.g. @docusaurus/plugin-content-docs/client, @docusaurus/theme-common).
# Hoisting keeps them resolvable while still linking every entry to the single
# copy in .pnpm, so shared React context stays intact.
shamefully-hoist=true
